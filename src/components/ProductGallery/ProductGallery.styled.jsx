import styled from '@emotion/styled';

export const Gallery = styled.ul`
  list-style: none;
  margin: 0;
  padding: 15px 0;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-gap: 15px;
`;
